<!-- awc:adapter=claude schema=1 -->
@AGENTS.md

# Claude Code adapter

Follow the shared workspace rules in `AGENTS.md`. Optional hooks under `.claude/` provide navigation context only; they do not replace source inspection or verification.
