# Orbit Notes — Synthetic Project Example

Orbit Notes is a fictional text parser used only to demonstrate project-scoped continuity. It contains no real customer, account, or production data.

```bash
python -m unittest discover -s tests -v
awc doctor . --strict
awc recover .
```
